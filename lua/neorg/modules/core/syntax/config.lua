---@class mod.syntax.config
local config = {
    -- note that these come from core.norg.concealer as well
    performance = {
        increment = 1250,
        timeout = 0,
        interval = 500,
        max_debounce = 5,
    },
}


return config
