---@class mod.upgrade.config
---@field public ask_for_backup boolean #Whether to prompt the user to back up their file every time they want to upgrade a `.norg` document
local config = {
    ask_for_backup = true,
}


return config
